const assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm');const app=fs.readFileSync('dist/app.js','utf8');const start=app.indexOf('function assignDestination('),end=app.indexOf('\ndocument.addEventListener',start);const paris={name:'Paris',country:'FR',center:{lat:48.85,lng:2.35}},london={name:'London',country:'GB',center:{lat:51.5,lng:-.12}};const t={destination:paris,days:[[{id:'a',budgetAmount:20,notes:'keep'}],[],[]],expenses:[{amount:10,currency:'EUR'}]};const ctx={trip:()=>t,destinationFor:i=>t.dayDestinations?.[i]||t.destination,TravelPlanner:{currency:c=>({FR:'EUR',GB:'GBP'})[c]}};vm.createContext(ctx);vm.runInContext(app.slice(start,end),ctx);assert(ctx.assignDestination(1,2,london));assert.equal(ctx.destinationFor(0).country,'FR');assert.equal(ctx.destinationFor(2).country,'GB');assert(ctx.assignDestination(0,0,london));assert.equal(t.days[0][0].budgetCurrency,'EUR');assert.equal(t.days[0][0].notes,'keep');assert.equal(t.expenses[0].currency,'EUR');assert(!ctx.assignDestination(2,1,paris));assert(!ctx.assignDestination(0,3,paris));assert.equal(t.days.length,3);console.log('PASS: day ranges, different countries in one trip, existing notes and expense currencies preserved.');
